name: audio_in_app
description: A Flutter plugin for playing audio files. Ideal for games or applications with sound.
version: 1.0.1
homepage: https://github.com/Cubel89/audio_in_app/tree/1.0.0

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^1.0.1
  universal_io: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter